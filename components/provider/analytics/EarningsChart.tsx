// Re-export the shared EarningsChart component for backward compatibility
// When using the simple variant, pass data prop with SimpleChartData format
export { EarningsChart } from "@/components/shared/charts/EarningsChart";